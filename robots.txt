# RadScheduler hosts a private practice scheduler. Public crawling adds
# nothing for users (the app surface is auth-gated) and creates load.
# Allow only the public root so a search-engine excerpt can show the
# splash; everything beyond requires a session anyway.

User-agent: *
Disallow: /

Sitemap:
